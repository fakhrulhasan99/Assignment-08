import React, { useEffect } from 'react';

const Contacts = () => {
    useEffect(() => {
            document.title = "Page Not Found | Doc Talk";
        })
    return (
        <div>
            contacts are here
        </div>
    );
};

export default Contacts;