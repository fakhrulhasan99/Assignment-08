import React, { useEffect } from 'react';

const Bookings = () => {
    useEffect(() => {
            document.title = "Bookings | Doc Talk";
        })
    return (
        <div>
            here are bookings
        </div>
    );
};

export default Bookings;